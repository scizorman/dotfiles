#!/usr/bin/env zsh
# NOTE: Uncomment when you want to take a profile
# zmodload zsh/zprof

typeset -U path manpath infopath fpath

export \
  XDG_CACHE_HOME="${HOME}/.cache" \
  XDG_CONFIG_HOME="${HOME}/.config" \
  XDG_DATA_HOME="${HOME}/.local/share"

export LANG='en_US.UTF-8'
